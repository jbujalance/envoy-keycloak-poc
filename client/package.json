{
  "name": "envoy-keycloak-poc-client",
  "version": "1.0.0",
  "description": "A client that makes an authenticated call to the proxified server",
  "main": "client.js",
  "scripts": {
    "start": "node -r dotenv/config client.js dotenv_config_path=../config/.env"
  },
  "author": "José Bujalance",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.0",
    "axios-oauth-client": "^1.3.0",
    "axios-token-interceptor": "^0.2.0",
    "dotenv": "^8.2.0"
  }
}
